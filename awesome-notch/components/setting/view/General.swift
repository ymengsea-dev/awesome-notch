import SwiftUI

struct General: View {
    var body: some View {
        Text("General")
    }
}

// notch size small(default), medium, large
// nothc color
// extend sensitiviti
// sync with mission(comming soon app)
// enable airdrop
// launch at login
